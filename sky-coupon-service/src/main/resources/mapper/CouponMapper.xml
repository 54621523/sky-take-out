<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.coupon.mapper.CouponMapper">

    <select id="pageQuery" resultType="com.sky.coupon.domain.po.Coupon">
        SELECT * FROM coupon
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <update id="decreaseStock">
        UPDATE coupon
        SET available_stock = available_stock - 1
        WHERE id = #{couponId} AND available_stock > 0
    </update>

</mapper>